apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tekton-testing

resources:
  - deployment.yaml
  - svc.yaml
  - route.yaml

images:
  - name: vote-ui
    newName: quay.io/openshift-pipeline/vote-ui
    newTag: latest
